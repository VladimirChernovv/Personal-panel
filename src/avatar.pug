include ../node_modules/bempug/index
- BEMPUG.modifier = '_';

//- blocks imports
include ./blocks/head/head
include ./blocks/header/header
include ./blocks/cover/cover
include ./blocks/user/user
include ./components/button_1/button_1
include ./blocks/personal-area/personal-area
include ./components/form/form
include ./blocks/footer/footer

doctype html
html
  +head({
    title: 'Аватар'
  })
  body.main
    
    +header({})
      
    main.main
      +cover({})
        +user({m: 'cover'})
        +personal-area({})
          //- +button({})

    +footer({})
    script
      include ./blocks/header/header.js